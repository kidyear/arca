# 灵匣 Arca · 公司版工作清单

> 本文档是上下文压缩后的接续依据。配合 Claude 记忆文件(fanbox-windows-port.md)使用。
> 仓库:company 分支(基于上游 alchaincyf/fanbox,已合并至 v1.7.2)。

## 一、产品定名(已决策,2026-06-12)

**灵匣 / Arca**——"文件夹操作 + Agent 结合体"的字面翻译:
- **匣** = 文件的容身处(文件匣)——文件管理这半边
- **灵** = 匣中有灵,会自己整理、替你办事——Agent 这半边
- 叙事:上游叫"翻箱"(人翻箱子),我们叫"灵匣"(匣子自己会干活),同宗进化
- 命名权重(用户定):6 分功能、2 分公司品牌(信步 SEAVO)、2 分文化底蕴
- **英文名 Arca**(2026-06-12 定,用户否决拼音 LingXia):拉丁语"匣/柜",与 Ark(约柜,"匣中有灵"的西方原型)同源;品牌区写法 SEAVO ARCA
- 可用标语:匣中有灵 / 文件匣,自己会干活
- 否决过的方向(别再提):拼音直转 LingXia、南桥/板桥(行业件名)、板载/点亮/常亮(黑话)、信手/闲庭/则灵(套用品牌文脉)、干将/搭手/办妥(帮手拟人)、总装/百工台/工坞(泛工作台)
- 候选过的英文名:Emberbox/Sparkcase/Tinderbox(用户选了 Arca)

## 二、改名执行清单(已完成,2026-06-12 提交 4cd6400)

**改(用户可见层)**:
- [x] package.json:name=arca、productName=灵匣 Arca、appId=com.seavo.arca、dmg title、win artifactName=Arca-Setup-${version}.exe、nsis shortcutName=灵匣 Arca
- [x] electron/main.js:app.setName('灵匣 Arca')、菜单 label、User-Agent 'fanbox-app'→'arca-app'
- [x] public/index.html:<title>、brand-name 灵匣、brand-sub(SEAVO ARCA)、ai-note 等"翻箱"文案
- [x] public/app.js:可见字符串(欢迎引导弹窗、toast、对话空状态等)里的 翻箱/FanBox
- [x] public/i18n-dict.js:FanBox 相关英文词条
- [x] ai.js:系统提示词「翻箱 FanBox」→「灵匣」、docx 编辑器默认 author
- [ ] WINDOWS.md/README 文档提名(低优先)

**不改(兼容优先,员工无感知)**:
- ~/.fanbox 配置目录、FANBOX_* 环境变量、fb_* localStorage 键、代码注释里的"翻箱"
- 理由:旧配置无缝沿用;减小与上游的合并冲突面

**待办(改名后续)**:
- [ ] 灵匣专属应用图标(现沿用箱形;SEAVO 设计系统 assets 有公司 logo 素材,可让 Claude Design 出一版"匣"形 icon,再走 build/icon.png→icns/ico 流程)

## 三、已完成的公司版能力(均已提交 company 分支)

1. **Windows 全适配** + NSIS 打包配置 + .github/workflows/build-windows.yml(Actions 出安装包)
2. **AI 对话面板**:Claude Agent SDK 内嵌引擎(能力=Claude Code 本体);Provider 全走 Anthropic 兼容端点(Claude 官方免 key 用本机登录/DeepSeek/Kimi/智谱/MiniMax/自定义中转,第三方用 ANTHROPIC_AUTH_TOKEN);审批闸门 canUseTool;左会话列表+续聊 resume+历史回显(读 ~/.claude/projects transcript);默认服务商 deepseek;引擎遥测外联全关
3. **任务模板**:templates.default.json(通用6+文控2+采购1+品质1),~/.fanbox/templates.json 可覆盖;卡片+填空表单;模板里格式规范是占位,待收集部门真实规范替换
4. **Office 预览编辑**:docx-editor(Word 原格式编辑保存,/api/write-binary)+ SheetJS(Excel 多表预览);vendor 构建 npm run build:docx / build:xlsx
5. **内网更新源**:FANBOX_UPDATE_URL / config.updateUrl → 内网静态 latest.json,详见 WINDOWS.md
6. **信步 SEAVO 皮肤**(默认):Claude Design 设计稿落地,DIN Pro 字体 vendor;设计包在 /tmp/fanbox-design(易失,设计系统源在用户的 Claude Design 项目里)
7. 小改进:侧栏最近打开(左键应用内开+右键菜单)、侧栏分组折叠、CSRF Origin 校验
8. **信步皮肤全保真**(2026-06-12):文件夹火焰橘红(iconColorFor seavo 分支)、侧栏底部 SEAVO 字标(vendor/brand/)、自有界面(对话/模板/AI设置/Office)在 seavo 下统一近直角(去胶囊/大圆角)
9. **zip 中文乱码修复**(上游 issue #9,我方先修):server.js zipCentralDirList 自己解析 zip 中央目录,UTF-8 标志位判断+GBK 回退解码,win/mac 同一条路且 Windows 也有单文件大小;解析失败退回 unzip/bsdtar
10. **Word AI 审阅**(2026-06-12,文控/品质/采购杀手锏):对话面板的 AI 能像人类审阅者一样给 .docx 打批注+修订(tracked changes),署名「灵匣 AI」,在 Word/灵匣预览里可逐条接受拒绝。实现:@eigenpal/docx-editor-agents 的 DocxReviewer(无头,后端直改文件)+ Agent SDK 进程内 MCP 工具(ai.js buildDocxServer):read/review_status 只读自动放行,add_comment/replace/review(批量,只弹一次审批)走审批门;整组全失败不落盘(防悬空批注)。E2E 已验:DeepSeek 跑通 读→批量审阅→预览可见
11. **AI relay 真机验证**(2026-06-12 Windows):npm start 网页版 + DeepSeek key 发「你好」真实对话已跑通，流式返回正常；修复点:SDK query 加 `settingSources: []`，避免用户全局 `~/.claude/settings.json` 里的 ANTHROPIC_BASE_URL/ANTHROPIC_AUTH_TOKEN 覆盖 Arca 自己的本机 relay 配置。

## 三点五、文件管理器对标(用户定位:基本替代资源管理器)

**一期已做(v1.0.4)**:Ctrl/Shift 多选+Ctrl+A;Ctrl+C/X/V 文件剪贴板(内部);Delete=回收站;
内部/跨窗口拖动=移动(Ctrl=复制),拖到文件夹卡片上=放入该文件夹;系统拖入=复制;右键组操作菜单+粘贴。
**对话并发模型**:busyChats 按会话记账,多会话并行各停各的,发送/停止只反映当前会话;后台运行的会话切走再切回显示"后台运行中"横幅(实时流不回放,完成后重开可见)。

**二期已做**:
- [x] 地址栏可输路径直达(2026-06-13):面包屑空白点击/双击进入输入态,Ctrl+L / Alt+D / F4 直达;支持绝对路径、`~`、相对家目录路径,提交复用 navigate,历史栈/watch/终端跟随都沿用。
- [x] 列表视图表头排序(2026-06-13):列表头「名称/修改时间/大小」可点击排序,同列再次点击切换升/降序;顶部排序按钮与表头共用状态,箭头提示当前方向。
- [x] 框选 marquee(2026-06-13):文件区空白处左键拖动出现选择框,网格/列表都可批量选择;Ctrl 拖动为追加选择;只切换 selected class,不重建文件列表。
- [x] OS 级文件剪贴板互通(2026-06-13 Windows 复制链路):资源管理器 Ctrl+C 的文件可在灵匣 Ctrl+V 粘贴到当前目录/右键文件夹;灵匣多选 Ctrl+C 同步写入系统 FileDropList,可到资源管理器粘贴。Ctrl+X 仍为灵匣内部移动语义,系统级剪切待补 DropEffect。
- [x] 拖出到资源管理器(2026-06-13 Electron):文件卡片/列表行/侧栏路径 dragstart 时调用 `webContents.startDrag({ file, files, icon })`,多选拖拽带整组 paths,可拖到桌面/资源管理器窗口。
- [x] 后台会话审批提醒(2026-06-13):AI 审批请求按会话记账,后台会话在左侧列表显示「待审批」徽标,对话按钮显示待处理数量;切回会话会重建可点击审批卡,允许/拒绝/超时后自动清理提醒。
- [x] OS 级文件剪贴板剪切 DropEffect(2026-06-13 Windows):灵匣 Ctrl+C/Ctrl+X 写入 FileDropList + Preferred DropEffect(copy=1/cut=2);读取资源管理器剪贴板时识别 copy/cut,Ctrl+V 分别复制/移动到当前目录。
- [x] 大目录分片渲染(2026-06-13):超过 420 项时先同步渲染首批 140 项,后续用 idle 分片追加;新目录渲染会取消旧批次,避免 1000+ 文件夹首屏长时间卡住或跳目录后旧 DOM 混入。
- [x] 键盘文件名定位与选择同步(2026-06-13):文件区直接键入文件名前缀可跳转,连续敲同一首字母循环匹配;方向键/Home/End/PageUp/PageDown 移动光标时同步选择当前项,避免键盘移动后 Delete/F2 作用到旧选择。
- [x] 原地重命名(2026-06-13):F2/右键重命名不再弹窗,直接在文件卡片/列表行名称处编辑;文件默认选中不含扩展名的主体,Enter/失焦提交,Esc 取消,更贴近 Windows 资源管理器。
- [x] 新建文件夹快捷键与原地命名(2026-06-13):Ctrl+Shift+N 在当前目录创建唯一占位文件夹并立即原地命名;右键新建文件/文件夹也走占位+原地命名,新建文件全选占位名便于直接输入 `note.md` 这类完整文件名。
- [x] 刷新与复制路径快捷键(2026-06-13):F5 刷新当前目录;Ctrl+Shift+C 复制所选/光标项完整路径,多选时按换行复制整组路径;桌面版走 Electron clipboard,避开浏览器剪贴板权限限制。
- [x] Explorer 导航历史(2026-06-13):增加前进栈和顶部前进按钮;Alt+Left/Alt+Right/Alt+Up 分别后退/前进/上一级,手动跳转新目录会清空前进栈,符合 Windows 文件管理器习惯。
- [x] 选择统计状态栏(2026-06-13):选中文件/文件夹后底部状态栏实时显示已选数量、文件夹数、文件总大小,并保留当前目录总览;Esc 多选态优先清选择再关预览,避免批量操作前状态不清。
- [x] 键盘上下文菜单(2026-06-13):支持 Shift+F10 / ContextMenu 键在当前光标项打开右键菜单,无当前项时打开空白菜单;菜单项文案改为中性的「在文件管理器中显示」。
- [x] 键盘范围选择(2026-06-13):Shift+方向键/Home/End/PageUp/PageDown 按固定锚点扩展或收缩连续选择;普通方向键退出范围选择并同步当前项,状态栏实时反馈数量/大小。
- [x] Alt+Enter 属性面板(2026-06-13):支持单项/多选属性查看,展示类型、位置、大小、修改/创建时间、完整路径;可复制路径,文件夹可直达占用透视。
- [x] Ctrl+Z 轻量撤销(2026-06-13):支持撤销最近一次重命名与新建文件/文件夹;新建后内联改名只记录最终路径,已用真实浏览器验证磁盘回滚。
- [x] Ctrl+F 当前目录搜索(2026-06-13):顶部增加当前目录过滤框,Ctrl+F 聚焦、输入即时过滤、Esc/清空按钮恢复列表;进入新目录/最近修改视图会强制清空旧关键词,避免误判为空目录。
- [x] Alt+P 预览窗格 + Ctrl+滚轮图标缩放(2026-06-13):Alt+P 对当前文件开关预览窗格,关闭时保留选择;网格视图 Ctrl+滚轮按 Windows 习惯调整小/中/大图标并同步工具栏按钮。
- [x] Explorer 单击/预览模型(2026-06-13):文件区单击只选中,双击/Enter 才默认打开;预览窗格需 Alt+P 显式开启,开启后随选择刷新,关闭预览不再丢失当前选择。
- [x] 缩略图按视口懒加载(2026-06-13):图片/视频项先渲染稳定占位,进入视口附近才写入 `/api/thumb` 请求;220 图片项 E2E 首屏仅请求 82 个缩略图,滚动后按需追加,降低大图片目录首屏压力。
- [x] Ctrl+Z 扩展到粘贴/拖放(2026-06-13):Ctrl+V 或拖放复制成功后撤销会把新副本移到废纸篓;剪切/拖放移动成功后撤销会移回原目录;已用真实文件 E2E 验证复制撤销、移动撤销。
- [x] Space/Ctrl+Space 选择语义(2026-06-13):Space 不再误触收藏,只保持/确认当前项选择;Ctrl+Space 切换当前光标项进入多选集合;收藏保留星标/右键入口,避免打断 Windows 选择肌肉记忆。
- [x] 反向选择/清空选择(2026-06-13):补齐 Ctrl+I 反向选择,空白/多选右键菜单提供全选、反向选择、清空选择;选择状态与底部统计同步,已用 4 文件 E2E 验证。
- [x] Esc 清选择优先级(2026-06-13):Esc 会先清多选/单选并同步清空预览窗格内容;无选择时第二次 Esc 才关闭预览窗格,避免选择状态与右侧预览错位。
- [x] Ctrl+D 删除语义(2026-06-13):Ctrl+D 改为对当前选择执行移到废纸篓,与 Delete/Ctrl+Delete 走同一条回收站链路;收藏不再占用删除快捷键,只保留星标/右键入口。
- [x] 侧栏目录右键菜单(2026-06-13):快速入口/收藏目录/Agent 项目支持右键打开、在文件管理器中显示、复制路径、终端打开、收藏/取消收藏、磁盘占用透视;最近打开旧 Finder 文案统一为文件管理器。
- [x] 侧栏祖先高亮(2026-06-13):侧栏 active 从“路径完全相等”升级为“当前目录落在入口之下则高亮最深祖先”,覆盖快速入口/收藏目录/Agent 项目,进入子目录仍保留当前位置感。
- [x] 顶部预览窗格按钮(2026-06-13):在视图切换旁增加预览窗格按钮,与 Alt+P 共用开关逻辑并同步 active/aria 状态;首次使用无需记快捷键也能发现右侧预览能力。
- [x] 同目录复制副本(2026-06-13):Ctrl+C/Ctrl+V 或粘贴 API 复制到原目录时自动生成 file-2.ext/Folder-2 副本,同时继续拦截把文件夹复制进自己;已用真实文件/文件夹和前端粘贴链路 E2E 验证。
- [x] Shift+Delete 永久删除(2026-06-13):补齐 Windows 习惯的绕过废纸篓删除;单选/多选都强确认,右键菜单提供永久删除入口,后端保护磁盘根目录;已用真实文件/文件夹 API 和前端快捷键 E2E 验证。
- [x] Backspace 优先后退(2026-06-13):Backspace 从“直接上一级”改为有历史时后退、无历史时上一级,贴近现代 Windows 资源管理器;已用真实临时目录验证历史后退与兜底上一级。
- [x] 大目录选择增量 repaint(2026-06-13):选中态/cursor class 从每次扫全列表改为只更新变化项,状态栏统计缓存当前目录总数并用 path 索引取选中项;900 文件目录连续 100 次键盘移动平均 0.744ms,保持 1 selected/1 cursor。
- [x] 空白右键当前文件夹操作(2026-06-13):空白处右键补齐刷新、复制当前文件夹路径、在文件管理器中显示、在终端打开、当前文件夹属性,保留新建/粘贴/选择/AI 整理;已用真实临时目录验证菜单和属性面板。
- [x] 空白右键收藏当前文件夹(2026-06-13):当前目录空白处右键可收藏/取消收藏当前文件夹,侧栏收藏立即同步,贴近资源管理器固定快速访问的入口;已用真实临时目录 E2E 验证。
- [x] 剪切视觉反馈(2026-06-13):Ctrl+X 后源文件/文件夹像资源管理器一样淡化显示,复制会清掉剪切态,剪切粘贴完成后自动移除标记;已用真实文件 E2E 验证 DOM class 与磁盘移动。
- [x] 粘贴后选中新项(2026-06-13):Ctrl+V 复制/剪切移动成功后刷新并自动选中新生成或移动到当前目录的项目,cursor 同步到结果项;已用真实文件 E2E 验证同目录副本和跨目录移动。
- [x] 拖放后选中结果项(2026-06-13):应用内/跨窗口拖放复制或移动到当前目录后自动选中新位置项目,cursor 同步结果项;浏览器版文件写入也记录 undo 路径;已用真实文件 E2E 验证复制和移动。
- [x] 删除后保留连续操作焦点(2026-06-13):移到废纸篓/永久删除刷新后自动选中邻近项目,删中间选下一个、删末尾选前一个,cursor 同步;已用真实文件 E2E 验证连续永久删除。
- [x] Alt+Enter 当前文件夹属性(2026-06-13):无选中/无光标项时 Alt+Enter 打开当前文件夹属性,与空白右键“当前文件夹属性”一致;有选中时仍显示选中项属性;已用真实临时目录 E2E 验证。
- [x] 当前文件夹属性真实时间(2026-06-13):新增只读 `/api/stat`,当前文件夹属性从真实文件系统读取 mtime/btime,不再显示占位 `—`;已用 API 和真实临时目录 Alt+Enter E2E 验证。
- [x] 地址栏 Windows 环境变量(2026-06-13):后端路径解析支持 `%TEMP%`/`%USERPROFILE%`/`%APPDATA%` 等 `%VAR%` 展开,地址栏可直接跳系统常用目录;已用 API 和地址栏 E2E 验证 `%TEMP%`。
- [x] 地址栏复制路径兼容(2026-06-13):后端路径解析支持资源管理器“复制为路径”的带引号路径和 `file://` URL,地址栏可直接粘贴这些路径跳转;已用 API 和地址栏 E2E 验证。
- [x] 面包屑拖放整理(2026-06-13):文件/文件夹可拖到面包屑任意祖先目录段,默认移动、按 Ctrl 复制,成功后跳到目标目录并选中新项;复用文件区拖放/撤销链路,已用真实临时目录 E2E 验证移动与复制。
- [x] 鼠标侧键导航(2026-06-13):支持 Windows 鼠标侧键后退/前进(button 3/4)和键盘 `BrowserBack`/`BrowserForward`,避开输入/编辑态,共用资源管理器历史栈;已用真实临时目录 E2E 验证。
- [x] Ctrl+Y 重做文件操作(2026-06-13):在现有 Ctrl+Z 基础上补 redo 栈,支持重命名、新建、复制、移动的 Ctrl+Y / Ctrl+Shift+Z 重做;新文件操作会清空 redo,失败时保留操作栈;已用真实文件 E2E 验证重命名和移动重做。
- [x] 压缩/解压 zip(2026-06-13):右键支持单项/多选压缩为 zip,zip/jar 支持解压到当前目录同名文件夹;Windows 走系统 PowerShell `Compress-Archive`/`Expand-Archive`,同名自动加序号,生成物接入 Ctrl+Z/Ctrl+Y;已用 API 和浏览器 E2E 验证。
- [x] 慢速二次点击重命名(2026-06-13):选中文件后再次单击文件名区域进入原地重命名,双击仍优先打开/进入,Ctrl/Shift 多选不触发;已用真实浏览器 E2E 验证慢速点击与双击分流。
- [x] 列表类型列与类型排序(2026-06-13):列表视图补齐“类型”列和按类型排序,顶部排序段同步增加“类型”,首次点击按文本升序;已用真实浏览器 E2E 验证列头、排序方向和顺序。
- [x] 列表列宽拖拽记忆(2026-06-13):详情列表的修改时间/类型/大小列可拖动调整宽度,名称列保持弹性填满剩余空间;列宽写入本机 localStorage,刷新后保持;已用真实浏览器 E2E 验证。
- [x] 列表列宽双击自适应(2026-06-13):详情列表列分隔线支持双击按当前可见项内容自动适配宽度,结果复用列宽记忆写入本机;已用真实浏览器 E2E 验证修改时间列从 64px 自动适配并持久化。
- [x] Shift+右键复制为路径(2026-06-13):文件/多选/当前文件夹 Shift+右键显示“复制为路径”,复制结果按 Windows 习惯逐行带双引号,可直接粘贴到命令行或地址栏;已用真实浏览器 E2E 验证复制文本。
- [x] 地址栏完整文件路径定位(2026-06-13):地址栏粘贴普通或带引号的完整文件路径时,自动进入所在目录并选中该文件,与 Shift+右键“复制为路径”闭环;已用真实浏览器 E2E 验证带空格路径和带引号路径。
- [x] Alt+双击属性(2026-06-13):支持按住 Alt 双击文件/文件夹打开属性面板,多选集合内 Alt+双击打开整组选中项属性,不打断普通双击打开;已用真实浏览器 E2E 验证单文件和多选。
- [x] Ctrl+N 新窗口(2026-06-13):桌面版支持 Ctrl+N 在新窗口打开当前文件夹,文件夹/空白右键补“在新窗口打开”;新窗口通过 `targetPath` 首屏直达目标目录,网页版降级为新标签;已用契约检查和真实浏览器 targetPath E2E 验证。
- [x] Ctrl+W 关闭当前窗口(2026-06-13):桌面版补 `window:close` IPC 和 `fanboxWindow.close()`,非编辑态 Ctrl+W 关闭当前窗口,Windows 菜单增加“关闭窗口”;已用先红后绿契约检查和 JS 语法检查验证。
- [x] 大目录自然排序加速(2026-06-13):文件名/类型排序从逐次 `localeCompare(...,{numeric:true})` 改为复用 `Intl.Collator`,保持 Windows 自然排序(file2 在 file10 前);3000 项排序微基准从约 25.045ms/轮降到约 1.191ms/轮,已用真实浏览器 E2E 验证编号文件顺序。
- [x] 当前目录过滤加速(2026-06-13):目录加载/刷新/最近修改结果预计算 `_searchName`,过滤时复用缓存而非每个字符都对全量文件名 `toLocaleLowerCase`;8000 项过滤微基准从约 3.430ms/轮降到约 0.524ms/轮,已用契约检查验证热路径,真实浏览器确认目标目录加载正常(浏览器工具文本输入受虚拟剪贴板限制,未声称键入 E2E)。
- [x] 重命名改扩展名确认(2026-06-13):已有文件重命名若改变扩展名,提交前弹出 Windows 习惯的风险确认,取消时不调用 `/api/rename`;新建文件原地命名跳过该警告,保留直接输入 `note.md` 的顺滑感;已用先红后绿契约检查和 JS 语法检查验证。
- [x] 多选状态栏统计缓存(2026-06-13):选中统计在选择变更/目录刷新时计算一次并缓存到 `state.selectionStats`,状态栏渲染只读缓存,避免大目录全选后每次刷新都遍历选中项;10000 项微基准从约 0.7412ms/轮降到接近 0ms/轮,已用契约检查验证。
- [x] 选中态 DOM 查找加速(2026-06-13):文件项渲染时登记 `state.domByPath`,选中/剪切/cursor 绘制优先 Map 查找 DOM,只在缓存失效时兜底 selector;10000 项路径查找微基准从约 12.688ms/轮降到约 0.447ms/轮,已用契约检查验证。
- [x] 分片渲染 force paint 减负(2026-06-13):`paintSelection(true)`/`paintCutMarks(true)` 不再 broad query 全量 `.selected/.cutting` 后清掉再加回,重渲染/分片追加时直接复用元素初始 class 与 DOM 索引收敛;10000 项微基准从约 0.3977ms/轮降到约 0.0230ms/轮,已用契约检查验证。
- [x] 类型标签映射减负(2026-06-13):`kindLabel()` 的类型中文名映射提升为模块级 `KIND_LABELS`,避免类型排序/列表渲染高频调用时重复分配对象;10000 次标签读取微基准从约 0.2536ms/轮降到约 0.2242ms/轮,已用契约检查验证。
- [x] 剪切态查找 Set 化(2026-06-13):`Ctrl+X` 后淡化标记从每个文件项 `Array.includes` 改为维护 `state.fileClipSet` 并用 `Set.has`,避免大目录批量剪切渲染退化为 O(n²);10000 项/5000 剪切项微基准从约 1346ms/轮降到约 0.624ms/轮,已用契约检查验证。
- [x] 中键打开文件夹新窗口(2026-06-13):文件夹项支持鼠标中键在新窗口打开,复用 `openNewWindow(e.path)` 和桌面 `window:new` 桥接,不打断左键选择/双击进入;已用先红后绿契约检查和本地 served app.js 验证。
- [x] Ctrl+Enter 文件夹新窗口(2026-06-13):键盘焦点在文件夹上时 Ctrl+Enter 打开该文件夹新窗口,普通 Enter 仍进入目录,Ctrl+Enter 文件仍走编辑器打开;已用先红后绿契约检查和本地 served app.js 验证。
- [x] 当前项键盘操作兜底(2026-06-14):Enter/Ctrl+Enter/F2/Space/Ctrl+Space/Ctrl+Shift+C/删除/永久删除/属性/键盘菜单等当前项操作优先使用 cursor, cursor 缺失时回落到可见 selected,避免"明明选中却快捷键没反应";已用先红后绿契约检查验证。
- [x] F6 主区域焦点循环(2026-06-14):补齐 Explorer 式 F6/Shift+F6 在地址栏、文件区、当前目录搜索、预览窗格、对话/终端面板之间循环;文件区与预览正文可键盘聚焦并有焦点环,方便键盘党在大目录里不靠鼠标找入口;已用先红后绿契约检查验证。
- [x] Ctrl+滚轮 / Ctrl+加减/0 缩略图大小(2026-06-14):网格视图支持 Ctrl+滚轮连续缩放,并补 Ctrl++/Ctrl+- 调整、Ctrl+0 回中等大小的键盘等价入口;只在非编辑态文件区接管,不影响输入框/编辑器;已用先红后绿契约检查验证。
- [x] Esc 清当前目录搜索(2026-06-14):当前目录过滤生效后,即使焦点已回到文件区,Esc 也会优先清空过滤并回到完整列表,再按 Esc 才走清选择/关预览;避免键盘党为了取消搜索来回点搜索框;已用先红后绿契约检查验证。
- [x] Ctrl+Shift+数字视图切换(2026-06-14):补齐 Explorer 式视图快捷键,Ctrl+Shift+1/2/3 切大/中/小图标网格,Ctrl+Shift+6 切列表视图;按钮与快捷键共用 `setFileView`,避免状态不同步;已用先红后绿契约检查验证。
- [x] Backspace 修正文件名快速定位(2026-06-14):主区键入文件名前缀定位时,1 秒内按 Backspace 会先删掉一个 type-ahead 字符并按剩余前缀重新定位,删空后只清前缀;无前缀时仍按资源管理器习惯后退/上一级;已用先红后绿契约检查验证。

**二期待办**:暂无(继续按长期目标做性能/替代度增强)。

## 四、长期待办

- [x] docx-editor 的 AI Agent 工具包接对话面板:让 AI 直接给 Word 打批注/修订(已完成,见上面第 10 条)
- [ ] Word 审阅延伸:模板库加「合同审阅」「文档规范检查」卡片,直接吃这套工具
- [ ] 收集各部门格式规范(周报/8D/命名规则/释放清单),替换模板占位提示词
- [ ] 种子用户试点(文控+采购+一名测试工程师),一周后按 chats.json 真实指令校准模板
- [x] 推 GitHub 私库跑 Actions 出 Windows 安装包(2026-06-12 完成,见下方发版流程)
- [ ] Windows 真机验收(已验:SDK spawn 引擎 + AI relay + DeepSeek 真实对话;待验:终端中文、缩略图、回收站、内网更新检测)
- [ ] 企业代码签名证书(消除 SmartScreen 提示,不着急)
- [ ] Excel 编辑(远期)
- [ ] SDK cost 显示按 Claude 价目估,第三方模型不准(可隐藏或换算)
- [ ] NAS/网络盘文件夹访问(上游 issue #5 的用户呼声;公司文控服务器共享盘场景适用)
- [ ] (可选)把 zip GBK 乱码修复回馈上游 PR(开源回报,也减小日后合并冲突)

**上游动态(2026-06-12 查)**:代码已同步到 v1.7.2(无新提交);开放 issue 仅 3 个——2 个求 Windows 版(我们已领先做完)、1 个 zip 中文乱码(本次已修);透明 PNG 棋盘格(issue #1)我方已有。

## 五、发版流程(v1.0.0 已于 2026-06-12 走通)

- **私库**:https://github.com/kidyear/arca(private,main=company 分支)
- **发新版**:改 package.json version → commit → `git tag v新版本 && git push seavo main --tags` → Actions 自动构建并发 Release(workflow 已配 contents:write)
- 若 tag push 没触发(新仓库偶发),手动 dispatch:`POST /repos/kidyear/arca/actions/workflows/build-windows.yml/dispatches {"ref":"v标签"}`
- **内网更新源**:公司内网服务器(Windows Server + IIS),发布目录放 Arca-Setup-X.Y.Z.exe + latest.json({"version","url"});客户端默认源由 CI 从仓库变量 COMPANY_UPDATE_URL 注入(electron/company.json,gitignored),不写进源码。实际地址见 GitHub 仓库 Settings→Variables
- **踩过的坑**:nsis 块不认 writeUpdateInfo(dmg 专属);新仓库 GITHUB_TOKEN 默认只读要加 permissions: contents:write;GitHub OAuth 推 workflow 文件需要 workflow scope(设备流授权)
- **v1.0.2 真机大坑(asar spawn)**:SDK 引擎二进制(claude.exe,平台包 claude-agent-sdk-win32-x64)被 SDK 从 app.asar 逻辑路径启动——fs 读有 Electron asar 透明转发所以"exists",spawn 没有转发所以"failed to launch"。修法:ai.js engineBinaryPath() 解析真实 unpacked 路径显式传 pathToClaudeCodeExecutable;asarUnpack 必须含 claude-agent-sdk-*(平台包,嵌套/平铺两种布局)。Anthropic 兼容端点多不实现 /v1/models→模型列表走各家 OpenAI 风格 modelsUrl 回退。NSIS 升级安装免卸载,配置在 ~/.fanbox 与安装目录无关
- **v1.0.5 真机大坑(全局 Claude 设置污染 relay)**:用户机器的 `~/.claude/settings.json` 可能配置了 `env.ANTHROPIC_BASE_URL`/`ANTHROPIC_AUTH_TOKEN`(本机中转、代理、其他模型等)。SDK 默认读取 user/project/local settings，优先级会覆盖 Arca 传入的 relay 环境变量，表现为仍然 `ConnectionRefused` 且 `~/.fanbox/engine.log` 没有 `relay 上游失败`。修法:ai.js 的 query options 加 `settingSources: []`，嵌入式应用以 Arca 的 Provider 配置为准。

## 六、跟上游同步的固定流程

`git fetch origin --tags && git merge <新tag>` → 冲突热区:app.js/main.js/server.js/style.css(双方都高频改)→ **合并后必查**:① 新增 execFile 调用的 Windows 兼容性(上游 mac-only 开发) ② 我方可见文案是否被覆盖 ③ node --check 全部 + 桌面版冒烟 + 对话面板回归。
